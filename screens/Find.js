import {StyleSheet, Text, View} from 'react-native'

export const Find = () => {
    return(
        <View style={{flex:1}}>
        <Text>Wow</Text>
        </View>
    )
}

const styles = StyleSheet.create({
    backgroundVideo: {
        position: 'absolute',
        top: 0,
        left: 0,
        bottom: 0,
        right: 0,
    },
})